import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    media: {
        height: "100%",
        width:"100%",
        // objectFit:"contain"
    }
}));
